# ------------------------------------------------------------------
# FRONTEND CONFIGURATION
# ------------------------------------------------------------------

# Google Maps — browser key (referrer-restricted). VITE_ prefix exposes it to the app.
VITE_GOOGLE_MAPS_API_KEY=
