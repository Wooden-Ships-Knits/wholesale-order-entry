fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
SQLAlchemy>=2.0,<3
psycopg[binary]>=3.1,<4
alembic>=1.13,<2
simple-salesforce>=1.12,<2
google-api-python-client>=2.100,<3
google-auth>=2.30,<3
itsdangerous>=2.1,<3
email-validator>=2.1,<3
jinja2>=3.1,<4
weasyprint>=61,<67
# Admin reports (DTO/DMM) reshape Salesforce report data with pandas.
pandas>=2.2,<3
# Conflict-reply classifier (see app/ai/conflict_reply.py).
openai>=1.40,<2
matplotlib>=3.8,<4
pillow>=10,<12
# Prospect scoring (app/prospects/). The scraper parses a shop's own catalogue
# with BeautifulSoup over the lxml parser and fetches with requests. All three
# were already present in the dev venv by accident and declared nowhere, so the
# container built fine and would have failed on the first import.
beautifulsoup4>=4.12,<5
lxml>=5,<7
requests>=2.31,<3
