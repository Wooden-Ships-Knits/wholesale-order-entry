"""Prospecting and territory mapping helpers (notebook-facing).

Not imported by the FastAPI app — these are analysis tools that live inside the
package so they can reuse app.config, app.geo.conflict and app.salesforce.
"""
